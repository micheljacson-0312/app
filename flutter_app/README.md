Flutter App

Setup
1. Ensure Flutter SDK is installed
2. cd flutter_app
3. flutter pub get
4. Run on emulator (Android uses 10.0.2.2 to reach host)

Notes
- Update ApiService baseUrl in providers/providers.dart to point to backend
